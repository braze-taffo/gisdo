# GISdo GUI 重构审查报告

- **审查对象**：`src/gisdo/gui/` 未提交改动（10 个文件，+355 / −270 行）
- **审查日期**：2026-08-11
- **审查方式**：两轮 —— 首轮全面审查 + 对抗复核（逐条实测/程序化验证首轮论断）
- **结论**：GUI 逻辑无阻断性问题，可合入；建议跟进 1 个测试缺陷与若干低风险清理项

---

## 1. 变更摘要

本轮改动是一次**纯视觉/交互重构**，未触及任何 engine/agent 逻辑：

| 变更 | 内容 |
|---|---|
| 主题重构（theme.py） | 深色侧边栏（`SIDEBAR_*` 色系）+ 亮色工作区；统一圆角/间距/字体；`ACCENT_FAINT`、`SURFACE_ALT` 等新色板常量 |
| 导航（app.py） | 侧栏加宽至 220px、品牌区（G 徽标）、分组标题（工作区 / GIS 工具）、就绪状态卡深色化、点击状态行跳转 |
| 页面切换动画（app.py） | `QGraphicsOpacityEffect` + 150ms 淡入 |
| 对话页（chat.py） | 工具栏面板化（`chatToolbar`）、输入区合成器（`composer`）、引导卡升级 |
| 细节（widgets.py 等） | 预览区样式、JsonTreeView 动画、各页统一边距（24/18/24/20）、出图页控件定宽 |
| 行为变化 | 日志面板默认隐藏（视图菜单可切换）；出图页左右栏比例 5:3 |

---

## 2. 自动化检查结果

| 检查 | 结果 |
|---|---|
| `ruff check src/gisdo/gui` | ✅ 全部通过 |
| 主题常量悬空引用（改名后残留） | ✅ 无 —— `theme.BG / ACCENT_HOVER / DANGER_HOVER / LOG_BG` 等旧引用均已迁移 |
| `theme.__all__` 完整性（程序化校验） | ✅ 25 个名字全部有定义，**无缺失** |
| 图标名核查 | ✅ `send / warning / clear / scroll / chat / folder / cpu / search / package / image / gear` 全部存在 |
| `setObjectName("card")` 残留 | ✅ 无 |
| QScrollArea 全局样式波及面 | ✅ 仅 render.py、widgets.py 两处，均被 ID 规则或 inline 样式覆盖，无副作用 |
| `pytest` | ⚠️ **3 failed / 238 passed**（详见 §3.1，与本改动无关） |

---

## 3. 发现的问题（按严重性）

### 3.1 高 —— 测试缺陷（与本改动无关，但需修）

`tests/test_failure.py` 3 个失败全部为**日期硬编码**：

- `engine/versioning.py:19,31` 用 `date.today()` 生成路径后缀（今天 → `20260811`）；
- 测试写死 `20260808`（tests/test_failure.py:59, 82, 86, 89, 98, 110），**只在 2026-08-08 当天能通过**，之后任何一天运行都会失败。

**修复建议**：mock `gisdo.engine.versioning.date.today`（如 `unittest.mock.patch`），或在测试里动态生成与代码一致的日期，不要写死。

### 3.2 中 —— 页面切换动画的资源滞留（实测确认）

`app.py:276-302` `_animate_current_page` 的 cleanup 闭包挂在 `animation.finished` 上，而 **Qt 的 `stop()` 不触发 `finished`**（offscreen 实测复刻验证）：

```
stop() 后 finished 触发: False          ← cleanup() 永不执行
stop() 后 effect 仍挂载在 widget 上: True
```

**后果**（连续快速导航时）：
- 被中断动画的 `QGraphicsOpacityEffect` 残留在**旧页面**上（下次导航回该页时会被 `setGraphicsEffect(None)` 清理，不无限累积）；
- 被中断的 `QPropertyAnimation`（parent=self）滞留直到窗口关闭（每次中断 1 个，量级很小）。

**修复建议**：`_navigate_to` 或 `_animate_current_page` 开头，对旧动画先 `disconnect finished` 并手动摘除其 effect；或将 cleanup 逻辑改为「停止时也执行」：
```python
if self._page_animation is not None:
    self._page_animation.stop()
    self._page_animation.finished.disconnect()  # 避免旧 cleanup 干扰
```

### 3.3 低 —— 死代码 / 无效操作

| 位置 | 问题 | 建议 |
|---|---|---|
| theme.py:27,31 | `SUCCESS_BG`、`DANGER_HOVER` 定义并进 `__all__`，但 QSS 与代码均未引用 | 删除或补用途 |
| widgets.py:169 | `setObjectName("")` 不保证触发 QSS 重算（Qt 需显式 `style()->unpolish/polish`），"移除 emptyPreview 样式"意图可能不生效；pixmap 模式下无视觉影响 | 删除此行，或显式 unpolish/polish |
| theme.py 各处 | 魔法色值散落：`#24324A`、`#60A5FA`、`#AFC4E5`、`#C8DAF8`、`#F3D29B` 等 | 抽为主题常量，便于未来深色模式 |

### 3.4 低 —— 全局样式波及日志工具栏（行为变化）

theme.py:133 新增全局 `QToolButton { padding: 6px; border-radius: 7px; }` 及 `:hover` 浅蓝底规则，**会影响所有 QToolButton**，包括日志面板的 clear / scroll 图标按钮（app.py:220-233）—— 它们无 ID 规则覆盖，将获得 6px 内边距和悬停底色。属行为变化，可能是预期设计，建议确认。

### 3.5 提示 —— 视觉区分度（非 bug）

`_NavButton` 未选中态悬停时图标/文字均变亮白（app.py:63-70 + QSS `#navBtn:hover`），与选中态（`#24324A` 底 + 左侧蓝条）的区分主要靠底色与边条，可接受。

---

## 4. 对抗复核记录（首轮论断的验证结果）

| # | 首轮论断 | 复核方式 | 结果 |
|---|---|---|---|
| 1 | `theme.__all__` 与定义不一致（BG 等被改名/移除） | 程序化校验 `__all__` vs 模块属性 | ❌ **撤回** —— 25 个名字全部有定义；实际问题是 3.3 中的 2 个死常量 |
| 2 | 动画 `stop()` 不触发 `finished` → effect 残留 | offscreen 实测（PySide6 真实运行） | ✅ **成立**，但严重性下调：effect 不无限累积，仅 animation 对象滞留至窗口关闭 |
| 3 | 3 个测试失败与本次改动无关 | `git diff --name-only` + 源码核对 | ✅ **成立** —— 失败文件不在改动集，根因为测试硬编码日期 |
| 4 | 无悬空主题常量引用 | grep 全项目 | ✅ **成立** |

**对抗复核新增发现**（首轮遗漏）：§3.3（死常量、`setObjectName("")` 无效）、§3.4（全局 QToolButton 波及日志按钮）、图标/对象名/滚动区使用面核查（均通过）。

---

## 5. 结论与建议

**合入结论**：本轮 GUI 重构质量良好 —— 纯视觉变更、无逻辑回归、ruff 全绿、无悬空引用，**可以合入**。

**跟进清单**（按优先级）：

- [ ] **P1** 修复 tests/test_failure.py 日期硬编码（mock `date.today`），使 `pytest` 恢复全绿
- [ ] **P2** 动画中断时手动清理旧 effect / 断开旧动画（app.py:276-302）
- [ ] **P3** 删除死常量 `SUCCESS_BG` / `DANGER_HOVER`；处理 `setObjectName("")` 无效行
- [ ] **P3** 确认日志工具栏按钮接受全局 QToolButton 样式
- [ ] **P4** 魔法色值抽常量化（为深色模式铺路）
